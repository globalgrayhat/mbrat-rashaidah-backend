export enum DonationStatusEnum {
  PENDING = 'pending',
  PAID = 'paid',
  COMPLETED = 'completed',
  FAILED = 'failed',
  SUCCESSFUL = 'successful',
  CANCELLED = 'cancelled',
}

export enum DonationStatus {
  PENDING = 'pending',
  PAID = 'paid',
  COMPLETED = 'completed',
  FAILED = 'failed',
  SUCCESSFUL = 'successful',
  CANCELLED = 'cancelled',
}
